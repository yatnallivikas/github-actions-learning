name: My First Workflow

on: push

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print greeting
        run: echo "Hello Vikas, GitHub Actions is working!"

      - name: Show date
        run: date

      - name: List files
        run: ls -la
